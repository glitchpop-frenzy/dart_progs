// Define a function
printNumber(num aNumber) {
  print(
      'The number is $aNumber'); // Print to the console, (I know ; is missing)
}

// This is where the app starts executing.
void main() {
  var number = 42; // Declaring and initializing a variable
  printNumber(number); // Call a function
}
